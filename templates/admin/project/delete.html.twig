{% extends 'base.html.twig' %}

{% block header_title %}{{ 'project.headlines.delete'|trans }}{% endblock %}

{% block content %}
    {{ form_start(form) }}
    <div class="card">
        <div class="card-status bg-red"></div>
        <div class="card-header">
            <h3 class="card-title">{{ 'project.headlines.delete'|trans }}</h3>
        </div>

        <div class="card-body">
            <p>{{ 'project.delete.question'|trans }}</p>
            <p>
                ID: {{ project.id }}<br />
                <strong>{{ project.name }}</strong>
            </p>
        </div>

        <div class="card-footer">
            <a href="{{ path('project_index') }}" class="btn btn-outline-danger float-right">
                {{ 'project.buttons.cancel'|trans }}
            </a>
            <button class="btn btn-danger">
                <i class="fe fe-trash"></i>
                {{ 'project.buttons.delete'|trans }}
            </button>
        </div>
    </div>
    {{ form_end(form) }}
{% endblock %}
